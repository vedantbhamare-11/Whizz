import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com','imgs.search.brave.com'], // Add the external domain here
  },
};

export default nextConfig;
