import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['via.placeholder.com', 'plus.unsplash.com'], // Add allowed domains here
  },
};

export default nextConfig;
